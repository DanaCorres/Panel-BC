name: Actualizar panel de noticias

on:
  schedule:
    # 3 veces al día: 9am, 3pm y 7:30pm hora Baja California/Sonora (UTC-7)
    - cron: "0 16,22 * * *"
    - cron: "30 2 * * *"
  workflow_dispatch: {}  # permite correrlo manualmente desde la pestaña Actions

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Recolectar titulares
        run: python scripts/fetch_news.py

      - name: Curar con Claude y regenerar index.html
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/curate_and_render.py

      - name: Commit y push si hubo cambios
        run: |
          git config user.name "panel-bc-bot"
          git config user.email "actions@users.noreply.github.com"
          git add index.html raw_items.json data/today.json
          if ! git diff --cached --quiet; then
            git commit -m "Actualización automática del panel"
            git pull --rebase origin main
            git push
          else
            echo "Sin cambios que subir."
          fi
